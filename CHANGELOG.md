# CHANGELOG

## v4.4.7 — 更新が利用者に届かない不具合を修正（最終チェックで発見）

### 見つけた不具合：キャッシュ名を上げても古い画面が出続ける

サービスワーカーを有効にしたまま検証したところ、

```
キャッシュ名             gaswipelab-v4.4.5   ← 新しい
キャッシュの中の index.html   v4.4.4          ← 古い
画面に出るのも              v4.4.4
```

**原因**: `install` の `cache.addAll(STATIC)` はブラウザのHTTPキャッシュ経由で取得する。
そのためブラウザが持っている**古い index.html が、そのまま新しい名前のキャッシュへ焼き込まれる**。
これまで「キャッシュ名を上げれば新版が届く」としていたが、**それだけでは届かなかった**。

これまでの検証はすべてサービスワーカーを外して行っていたため、この経路を踏んでいなかった。

### 修正

**1. 取り込み時にHTTPキャッシュを迂回する**

```js
cache.addAll(STATIC.map(url => new Request(url, {cache: 'reload'})))
```

**2. 新版に入れ替わったら自動で読み直す**

修正1だけでは、初回の表示は古いまま（1回開き直す必要がある）。
起動直後に新しいサービスワーカーが有効になったら、自動で読み直すようにした。

- 初回インストール時は読み直さない（無限ループ防止）
- 起動から10秒を超えている＝**使用中**の場合は読み直さず、
  「新しい版があります。開き直すと反映されます」と案内だけ出す（入力を失わせない）

### 検証（実際の更新の流れをそのまま再現）

```
v4.4.6 を導入して制御下に置く
  → ディスクを v4.4.7 に差し替え（更新を配った状況）
  → 1回リロード
     表示           v4.4.7  ✓
     キャッシュ名    gaswipelab-v4.4.7  ✓
     キャッシュ内容  v4.4.7 の index.html  ✓
     サービスワーカー有効のまま全機能が動作  ✓
```

### 最終チェックの結果

```
配布物の完全性     sw.js の41ファイルすべて実在／index.html が要求するファイルの欠落なし
git               配布・案件とも作業ツリーは clean、GitHub と一致
テスト             ゴールデン30件・物理モデル・装置画面30項目  すべてPASS
学習済みモデル7件   SHA-256 が改修前と一致
サービスワーカー有効での起動  条件値/バー/数値欄の不一致なし
```

## v4.4.5 — 条件を触っている間も答えが見える途中経過バー

### 見つけた問題：答えが画面の外で更新されていた

条件設計タブを 375px 幅で測ったところ、

```
タブ全体の高さ        3,518 px
答えが始まる位置      2,162 px  ← 画面2.7枚ぶん下
```

入力（手順1〜3）を触ると答えは自動で計算し直されるが、
**その更新が画面の外で起きていた**。条件を変えても結果が見えないため、
「答えが出ない」「どこを見ればいいのか分からない」状態になっていた。

### 途中経過バーを追加した

条件設計タブの上部に、スクロールしても貼り付いたままのバーを置いた。

```
┌──────────────────────────────────────────┐
│ 目標 133.8 → 予測 133.6 g/m²（両面） -0.2  答えへ ↓│
│ 変えるのは Yノズル位置（表・裏とも）：16.30 → 15.17 mm │
└──────────────────────────────────────────┘
```

- 手順1〜3のどこを触っていても、**目標・予測・変えるべき条件が常に見える**
- 目標に届いていれば緑枠＋「いまの設定のままで目標に届きます」、
  変更が要るなら黄枠＋変更内容
- 押すと答えの全文へ飛ぶ
- 計算中は薄くなる。計算に失敗したときは消える（古い値を残さない）
- スマホは画面上端に、PCではヘッダー・モード切替・タブの下に貼り付く
  （タブ高さを実測して位置を決める）

### 検証

3巡目の全面検証。実挙動の不具合は**見つからなかった**。

```
起動 → ライン切替 → 記号切替 → 板厚変更 → 提案の反映  条件値/バー/数値欄の不一致なし
装置画面・条件比較・モデル比較・理論モデル4タブ        NaN/undefined の表示なし
条件比較の導線 → 振る → 基準に戻す                    正常
GLでモデル比較                                        GL警告が出る
375px 全9タブ                                         ページ横スクロール0
コンソールエラー                                       0
未定義の関数呼び出し・存在しないDOM id参照            0
ゴールデン30件・物理モデル・装置画面テスト30項目       すべてPASS
```

## v4.4.4 — 設定すべきノズル条件を出し直した（v4.4.3 の作り直しを修正）

v4.4.3 で「実機で変えるのはこの1つ」を主役にした結果、**実機で設定する値そのもの**
（Yノズル位置・ノズル圧力・吹込流量・通板速度）を小さなチップへ落としてしまい、
**設定すべき数字が読み取れなくなっていた**。この画面のいちばんの目的を損なう変更だった。

設定値を主役に戻したうえで、「変えるのは1つ」の情報も残す形にした。

```
この目標を出す操業条件
目標 105.0 g/m² に対する予測は 105.0 g/m²（両面合計）です。

実機で設定する値
┌ Yノズル位置（表） ┬ Yノズル位置（裏） ┬ ノズル圧力（表） ┐
│ 10.62 mm ▲3.02変更│ 11.12 mm ▲3.02変更│ 40.80 kPa       │
├ ノズル圧力（裏）  ┼ ノズル吹込流量    ┼ 通板速度         ┤
│ 40.70 kPa        │ 2347 Nm³/h        │ 120.0 m/min      │
└──────────────┴──────────────┴─────────────┘

このうち、実機で変えるのは1つだけ
Yノズル位置（表・裏とも）   7.60 → 10.62 mm  ＋3.02
```

- **6項目すべてを大きく表示**（見出し「実機で設定する値」）。設定するときはここだけ見ればよい
- 変える項目には「▲3.02 変更」を添える
- グリッドの下に「このうち、実機で変えるのは1つだけ」として変更前→変更後を再掲
- 変える条件が無いときは「実機で変える条件はありません／上の値は、いまの設定のままです」

375px でも横スクロールなし（2列表示）。

## v4.4.3 — 呼び方の平易化と、操業の使い方に合わせた画面の作り直し

### 「プレナム圧」をやめて「ノズル内部圧」にした

現場で使われない用語だったため、画面の文言を入れ替えた。

| 変更前 | 変更後 |
|---|---|
| 噴射圧力（プレナム圧） | **ノズル内部圧** |
| プレナムゲージ圧 | **ノズル内部圧（大気圧との差）** |
| プレナム室の圧力 | ノズル内部の圧力 |

解説には「文献では『プレナム圧』と呼ばれます」と1行だけ残した。
`plenum_pressure_kpa` という内部の名前は互換のため変えていない。

### 「どのノズル条件にすればよいか」を1つに絞って出すようにした

これまでは6項目（Yノズル位置 表裏・ノズル圧力 表裏・吹込流量・通板速度）を
同じ大きさの枠で並べ、変える項目だけ色を変えていた。
**どれを実際に触ればよいのかが分かりにくかった。**

実機で手を動かすのは1項目だけなので、それを主役にした。

```
実機で変えるのはこの1つ
Yノズル位置（表・裏とも）
7.60 → 10.62 mm  ＋3.02

そのままにする条件  ノズル圧力（表）40.80kPa ・（裏）40.70kPa ・
                    吹込流量 2347Nm³/h ・ 通板速度 120.0m/min
```

- 変更前→変更後と変化量を1行で読めるようにした
- 残りは「そのままにする条件」として小さくまとめた
- 変える条件が無いときは「実機で変える条件はありません」と明示する
- Yノズル位置は表・裏を同じだけ動かすので、ラベルを
  「Yノズル位置（表・裏とも）」にした（従来は「（表）」に見えて片側だけと誤解しやすかった）

### 「設定を調整したら付着量がどうなるか」への導線を作った

この使い方は「条件比較」タブで既にできたが、**そこへ行き着けなかった**。
条件設計は「目標 → 条件」、条件比較は「条件 → 付着量」で向きが逆であることも
書かれていなかった。

- 条件設計の提案に **「この条件から微調整して目付を見る」** ボタンを追加。
  押すとその提案を基準にして条件比較タブへ移り、すぐ振れる状態になる
- 条件設計の下に **「設定を変えたら付着量はどうなるか」** カードを常設し、
  提案が無いときでも「いまの条件から調整して確かめる」で移れるようにした
- 条件比較タブの見出しを「この画面は『もし〜したら』を試す場所です」から
  **「設定を変えたら付着量がどうなるかを見る」** に変更。
  条件設計と向きが逆であることと、
  **提案どおりに設定できないときに実際の設定値で確かめる**用途を明記した

## v4.4.2 — プレナム圧の説明・モデル比較の作り直し・物理モデルの精度検証

### 「噴射圧力（プレナム圧）」が何の圧力か説明した

コードを読んで定義を確定し、理論モデル＞条件設計タブに解説を追加した。

- **ノズル内部の空洞（プレナム室）の圧力**で、**大気圧との差（ゲージ圧）**
- この圧力差でガスが大気へ噴き出すので、噴出速度と鋼板に当たる圧力が決まる
- 実機のノズル圧力と**同じものを指すと考えられる**
  （実績の表ノズル圧力 33.6〜70.2 kPa・中央値 37.3 と同じ桁）
- ただし**圧力計の取付位置は未確認**なので、モデル比較では引き続き同じ値として扱わない

### モデル比較を作り直した

従来は「予測付着量・速度・板幅・浴温・圧力・位置」を1つの表に混ぜていた。
速度・板幅・浴温は**写した値なので必ず差ゼロ**で、読む意味がなかった。

- **比べる意味があるのは予測付着量だけ**なので、2つの答えを大きく対比して見せる
- 差が「両モデルの誤差を足した幅」のどこにあるかをバーで示す
- 「そろえた条件」はチップ表示、「そろえられない条件」は理由つきの表に分離
- 修正: 取り込み時の丸めが**刻み**基準だったため、板幅 1219 mm が 1220 mm になっていた。
  **表示桁**基準に変更（板幅の刻みは 10 mm だが、実際の板幅をそのまま使いたいため）
- 修正: 基準を控える順序が誤っており、計算しても表が出ないことがあった

### 物理モデルの精度を測れるようにした（`tools/evaluate_physics_model.py`）

README にあった「物理モデル単体の MAE は GI 約12.2」は**再現手順が残っていなかった**。
実測と突き合わせられる評価環境を作り、測り直した。

実績にはノズルすき間と真の距離の記録が無いため、`Z = 設備Y座標 + オフセット` として
オフセットとすき間を学習期間で当てはめ、後の期間で評価する。

```
GI 7,076コイル → 学習 4,527 / 評価 1,500（2026-05〜07）
当てはめ: オフセット −2.0 mm / すき間 1.4 mm
評価期間 MAE 7.21 g/m²（片面）＝ 14.4 g/m²（両面）
実績モデル（GI）は 6.7 g/m²（両面）なので、理論モデルは約2.1倍の誤差
```

### 精度向上：実績校正が効く（両面 14.4 → 9.8 g/m²・32%減）

**アプリに元からある「実績校正」タブの6係数当てはめが、いちばん効く。**
学習期間150行で当てはめ、学習に使っていない期間で評価した結果。

| | 片面 MAE | 両面 MAE | P90 |
|---|---:|---:|---:|
| 校正なし（現行の出荷状態） | 7.21 | **14.4** | 13.4 |
| 実績校正あり | **4.89** | **9.8** | 10.1 |

抽出を変えた1,500行でも再現した（4.86〜4.89）。校正後も物理的な向きは保たれている
（圧力↑→付着量↓ / 距離↑→↑ / 速度↑→↑ / 浴温↑→↓）。

`tools/evaluate_physics_model.py --export out.csv` で、
「実績校正」タブへ読み込ませる形式のCSVを書き出せるようにした。

**出荷時の既定値には入れていない。** 理由:
- この係数は**このラインのGI**に、当てはめたすき間 1.4 mm・オフセット −2.0 mm という
  前提ごと合わせたもの。前提が変われば合わなくなる
- `scale_factor` が探索範囲の上限（10.0）、`speed_factor` が下限近く（0.13）に張り付いており、
  係数どうしが打ち消し合っている。**個々の数値に物理的な意味は無い**
- 校正は利用者が自分のデータで掛けるもの、という元の設計を変えない

### 精度向上を試したが、採用しなかったもの

文献調査（Mendez et al. 2020, *J. Fluid Mech.*「Dynamics of the Jet Wiping Process via
Integral Models」ほか）から、現行の液膜モデルは**慣性・移流を無視した0次モデル**にあたり、
慣性を入れると膜厚の応答指数が **−0.5 → −0.63〜−0.68** に立つことが分かった。
現行モデルの実測応答指数は **−0.55** で、確かに0次モデルの側にある。

**しかし実機データで検証したところ、どの補正もホールドアウトでは悪化した。**

| 試した補正 | 学習MAE | 評価MAE |
|---|---:|---:|
| 補正なし（現行） | 7.380 | **7.297** |
| 応答指数を立てる（文献の慣性補正・k=0.06） | 7.242 | 7.434 |
| 応答指数を立てる（文献推奨 −0.65 相当） | 7.519 | 8.099 |
| ノズル距離への感度を強める | いずれも悪化 | いずれも悪化 |
| 全体スケール＋オフセットの校正 | 7.013 | 7.438 |

**すべて学習期間だけ改善し、評価期間では悪化した。** 過学習の兆候であり、
モデルの式の誤りが原因ではないと判断して、**いずれも採用しなかった**。

残差は、記録が無い量（コイルごとのノズルすき間・真の距離・ノズル摩耗）の
ばらつきが支配的と考えられる。

なお調査の過程で、**先に報告した「付着量が小さいと厚めに出る／大きいと薄めに出る」は
統計的な誤り**だと判明した。実測値で層別すると、誤差のあるモデルでは必ずこの形になる
（回帰の希薄化）。入力側で層別し直すと、この傾向は大きく弱まる。

### 文献調査で確認できたこと

- **Elsaadawy et al.(2007) の圧力・せん断相関は現在も標準**。2020年の JFM 論文でも
  ワイピングアクチュエータとしてそのまま採用されている。差し替える必要は無い
- 表面張力項の追加は、強ワイピング条件では最終膜厚にほとんど効かない（同論文）
- エッジオーバーコートの定量式（Kim et al. 2003, ISIJ Int. 43(10) 1495）はあるが、
  **3次元の流れ場が必要**で、1次元の現行モデルには組み込めない

### 検算した式（いずれも問題なし）

```
圧力勾配 = 圧力分布の解析微分     相対誤差 1e-9（数値微分と比較）
近接場/遠方場の接続（Z/d=8）      ピーク値・分布とも連続
付着量の Z/d 依存                6〜11 で飛びなし（1ステップ最大 0.25%）
物理的な向き                      圧力↑→付着量↓ / 距離↑→↑ / 速度↑→↑ / 浴温↑→↓
```

せん断分布の分岐点 ξ=1.73 に 0.02% の段差があるが、これは2つの公表式の継ぎ目であり、
なめらかにすると文献値から外れるため触っていない。

## v4.4.1 — 全体デバッグで見つけた不具合の修正

挙動を総ざらいして見つかったものを直した。**予測モデル本体と物理モデルは無変更**
（ゴールデン30件 最大差 5.684e-14／物理モデル 34.3 kPa・8.0 mm のまま）。

### 予測が黙って変わっていた不具合

**1. 区分（カテゴリ）を選ぶと値が数値へ化ける**

`setMlField()` が区分の選択も数値欄と同じ経路で受けており、`parseFloat` を通していた。
`parseFloat('4N')` は `4` になるため、**類別で `4N` を選ぶと、モデルには存在しない区分「4」**
が渡っていた。エラーも警告も出ないまま予測値だけが変わる。
区分専用の `setMlCat()` を追加し、文字列のまま入れるようにした。
`Y:類別` は学習済みモデルの特徴量（26項目のうちの1つ）であることを `manifest.json` で確認済み。

あわせて、**区分を変えても答えが計算し直されていなかった**（`setMlField` は再計算を呼ばない）。
`setMlCat()` から再計算するようにした。

**2. 公開用の参照データ（`--redact`）にすると実績モデルが全滅する**

`combination_distance()` が件数を `envelope["n"]` と直接参照していた。`--redact` は件数を落とすため、
`ml_predict` / `ml_design` / `ml_compare` / `hmi_screen` がすべて `KeyError: 'n'` で失敗する。
`.get("n")` に変更し、件数が無いときは警告文から件数を省くようにした。
公開用データで通常操作が通ることを確認済み。

**3. 表示している値と計算に使う値が違う**

- 実績中央値がバーの刻みに乗っていない項目があった（ノズル高さ 399.65 に対し刻みは 0.1）。
  画面は 399.6、計算は 399.65。`snapMlCondToSliders()` で刻みへ揃えるようにした。
  予測への影響は実測 **0.0000 g/m²**（該当は3項目9ケース）
- 数値欄に範囲外の値（例 9999）を打つと、値は上限へ丸められて再計算されるのに、
  **欄は 9999 のまま**残っていた。確定時に実際の値へ直すようにした
- 「実績モデルの条件を取り込む」が範囲クランプだけで刻みに乗せていなかった。
  欄に「132」と出て計算は 132.4、という食い違いが起きる。取り込み時に刻みへ揃えるようにした

### 古い結果が新しい条件のもののように見える不具合

**4. モデル比較タブが、条件を変えた後も古い理論モデルの答えを並べる**

条件設計で板厚を 0.40→1.6 mm に変えてモデル比較タブへ戻ると、実績モデルの予測だけ
新しくなり、理論モデル側は前回の条件（速度 120 m/min）のまま並んだ。
そのうえで「**差 0.2 g/m² は誤差の内側です。2つの見立ては矛盾していません**」と表示していた。
**違う条件で計算した2つの答えを比べて「一致」と言っていた**ことになる。
両モデルの条件のスナップショットを持ち、変わっていたら表と判定を出さずに再計算を促す。

**5. 条件比較タブの結果カードが、基準を変えても残る**

「まだ何も変えていません」と出ているのに、前回の「+8.2 g/m²」が残っていた。
基準が変わったら結果を取り下げる。

**6. 計算に失敗しても前の答えが残る**

条件設計・条件比較で計算が失敗したとき、トーストが2.2秒出るだけで
**前回の答えが画面に残り続けていた**。失敗した旨をその場に表示するようにした。
装置画面も、失敗時に空白のままだったのを理由を出すようにした。

### 操作しても効かない・分からない箇所

- **詳細パネルの板厚・板幅では通板速度が入れ替わらなかった**。同じ項目が手順1と詳細パネルの
  2箇所にあり、詳細パネル側だけ v4.4 の自動反映に繋がっていなかった。両方を同じ経路にした
- **自動入力した速度の説明が陳腐化していた**。速度を手で動かした後や提案を反映した後も
  「○○を入れました」「元の値に戻す」が残り、押すと2手前の値へ飛んだ。取り下げるようにした
- **ヘッダーの校正バッジから実績校正タブへ飛ぶと、ナビがどのタブも光らない状態**になった。
  モードが実績モデルのままで、実績校正タブのナビが隠れていたため。
  別モードのタブへ飛ぶときはモード表示も合わせるようにした
- **生産仕様No. を変えても予測は動かない**（学習済みモデルの特徴量ではない）。
  そう明記した。他の7区分はモデルの入力

### そのほか

- `loadCSV()` だけが Python 呼び出しの直列キューを迂回していた。共有グローバル `_arg` を
  取り合う可能性があるため、`pyRaw()` を足して同じキューに乗せた。
  ファイル読取も try の中へ入れた（失敗すると「読み込み中…」で固まっていた）
- 起動処理の後半で失敗すると、ナビも無く理由も出ない空画面になっていた。
  条件の初期化を画面表示より前へ移し、失敗時は起動画面を出し直して理由を書くようにした
- `resetCalibration()` に失敗時の表示が無かった（リセットできたと誤解される）
- `fit_speed_by_size()` で板厚・板幅がほぼ一定のラインだと帯番号が -1 になり得た。
  現行データでは起きないが、境界が1点に潰れたら速度表を作らないようにした
- `sw.js` の `CACHE` を `gaswipelab-v4.4.1` へ。
  **上げないと、すでに v4.4 を開いた端末には今回の修正が届かない**

## v4.4 — 初期画面の固定・速度の実績反映・検証タブの解説・モデル比較タブ

使い勝手の改善。**予測モデル本体（`models/GI_*.json` / `GL_*.json` / `manifest.json`）と
物理モデルは無変更。** 変えたのは初期値・参照データ・画面の説明であって、予測の計算ではない。

### 起動時は必ず「実績モデル ＞ 条件設計」から始まる

前回開いていたタブとモードの記憶をやめた。開いた直後にすることが毎回同じになる。

- `mlState` の既定を `tab:'mldesign'` に変更
- `saveState()` の保存対象から `mode` / `tab` を外した
- `loadState()` は、旧バージョンが保存した `mode` / `tab` を捨ててから復元する
  （捨てないと、既存ブラウザでは保存値に負けて初期画面が変わらない）
- ライン・付着量記号・目標値・記号変更フラグの記憶は今までどおり

### 板厚・板幅を変えると通板速度も実績にあわせて入れ替わる

板厚は速度と強く連動している（実績の相関 GI **−0.863** / GL **−0.688**、
板幅は GI −0.146 / GL −0.416）。サイズだけ変えて速度を記号別中央値のまま置くと、
実在しない組合せの条件になっていた。

速度の当て方を5分割交差検証で比較した（平均絶対誤差 m/min）。

| 方式 | GI | GL |
|---|---:|---:|
| 付着量記号の中央値（v4.3まで） | 18.27 | 17.06 |
| 板厚×板幅グリッド（分位点だけで帯を切る） | 8.56 | 7.80 |
| **板厚×板幅グリッド（分位点＋画面のプリセット値。採用）** | **8.21** | **6.84** |
| 板厚×板幅の近傍25件（不採用・生データ配布が必要） | 7.07 | 6.99 |

記号ごとに表を分けても改善しない（GI 7.02 ≒ 7.07 / GL 7.31 は悪化）ため、ライン単位の表とした。

**帯の境界には画面のプリセット値（板厚 0.27〜3.2 / 板幅 610〜1350）を必ず入れている。**
分位点だけで切ると、点数の少ない厚物が **[1.6, 3.2] mm** という1つの帯に潰れ、
その中央値が 3.2 mm のコイルにも返っていた（GI 63.4 m/min。実測は 1.6〜1.9 mm が 65.0、
1.9〜2.4 mm が 50.0、2.4〜3.2 mm が 40.0、3.2 mm ちょうどが **37.0**）。
プリセットを境界に足すことで、板厚2.3 mm は 63.4 → **50.0** と実測（46.2）に近づいた。

薄物側は非単調のまま（GI で 0.27 mm 115 → 0.40 mm 120）。これは実データの性質で、
板幅1150〜1350帯では 0.25〜0.80 mm の速度がどれも 120〜135 とほぼ横ばい（ラインの上限に張り付いている）。
**平滑化や単調化はしていない。**

- 追加: `tools/build_reference_data.py` の `fit_speed_by_size()` →
  `reference.json` の `lines[line].speed_by_size`（板厚帯×板幅帯の中央値と件数）
- 追加: `RangeChecker.speed_for_size()` — セル → 同じ板厚帯の近い板幅 → 板厚帯 →
  ライン中央値、の順に落とす。**外挿はしない**
- 変更: `RangeChecker.defaults()` — 初期速度も固定サイズ 0.40 mm × 1219 mm の実績から決める
  （旧: 記号別中央値。例えば GI 記号「122」は 57.0 m/min だが、
  0.40 mm × 1219 mm の実績中央値は 125.0 m/min で噛み合っていなかった）
- 追加: `web_api.ml_speed_hint`、`ml_defaults` の戻り値に `speed_hint`
- 画面: 手順3に「板厚 ○ mm × 板幅 ○ mm に近い実績 **n 件**の中央値を入れました」と根拠を表示し、
  **1クリックで直前の速度に戻せる**。実績の外側のサイズでは端の帯の値を出して外側である旨を明示

**予測モデルの入力項目・特徴量・重みは変えていない。変わるのは初期値だけ。**

### 検証タブに「何をどう読むか」を追記

要素は揃っていたが、各カードに読み方の説明が無かった。

- タブ冒頭に読む順番（①予測結果 → ②リスク判定 → ③感度 → ④マップ）
- ①予測結果: 片面／両面の別、適正・未達・過剰の基準（**達成率 90%未満／110%超**）、推定範囲の意味
- ②リスク判定: **「モデル信頼度」と「推定ばらつき ±%」は実測との誤差ではない**ことを明示。
  文献の適用条件からの外れ具合を点数化した見積り（基準 ±12% ＋条件ごとの加算、
  合計 18% 以下＝高／30% 以下＝中／それ超＝低）であり、
  実績モデルの誤差（GI 6.7 / GL 3.2 g/m²）とは別物なので足したり比べたりしないこと
- ③感度グラフ・④マップ: 使い方（管理幅の決め方・運転点の選び方）を追記
- ④計算内訳: 各量の意味と見るべき点（無次元流束 Q は上限 0.6667 に近いほど効いていない、など）
- ⑤モデルからの注意: 自動生成の警告である旨

### 「モデル比較」タブを新設

比較機能自体は v4.2 からあったが、理論モデル＞条件設計タブの下部に隠れており、
計算ボタンを押すまで出てこなかった。

- `tab-modecmp` を新設。**実績モデル側・理論モデル側の両方のメニュー**から開ける
- ボタン1つで「条件を写す → 両方のモデルを計算 → 並べる」まで完結
- **そろえられる条件／そろえられない条件の一覧**を追加（分かりにくさの核心はここだった）
- 追加: 両面合計の作り方の違い（理論は片面×2の対称仮定、実績は CF と CG の独立予測の和）。
  **合計が近くても表裏の配分まで合っている保証はない**
- 修正: ノズル圧力の行が、`importFromMl()` が意図的に写していないにもかかわらず
  差分を数値で出していた。ノズル位置と同じく注記を付け、差を出さないようにした
- 既存の判定文・GL警告・「どちらのモデルにも入っていない実機の変動要因」はそのまま使用

### そのほか

- `sw.js` の `CACHE` を `gaswipelab-v4.4` へ。
  上げないと作り直した `reference.json` が古いままキャッシュに残る
- 使い方タブに ⑧モデル比較 の説明を追加。①条件設計に速度の自動反映の説明を追加
- 修正: 自動で入れた速度が、バーの刻み（0.1）に丸められる前の値のまま条件に入っていた。
  画面が 64.9 なのに計算は 64.92 でする食い違いが起きるため、刻みに合わせてから条件へ入れるようにした
- `tests/test_hmi_screen.py` に4件追加（25 → 30項目）。
  板厚から速度が引けること／実績外でも外挿しないこと／初期速度が固定サイズの実績と一致すること／
  速度の表が無い参照データでも落ちず記号別中央値に戻ること

### 検証結果

```
ゴールデン30件 一致（最大差 5.684e-14）／本家CatBoostパリティ 1000行 一致
物理モデル 34.3 kPa / 8.0 mm（不変）
装置画面テスト 30項目すべてPASS
学習済みモデル7ファイルの SHA-256 が改修前と一致
375px 全9タブ ページ横スクロール0 / DOM ID重複0 / コンソールエラー0
```

## v4.3 — 統計的な言い方の是正と、実績範囲判定の強化

外部レビューの指摘を検証し、妥当と判断したものを反映した。
**予測モデル本体（models/*.json）と物理モデルは無変更。**
ゴールデン30件は最大差 5.684e-14 で一致、物理モデルは 34.3 kPa / 8.0 mm のまま。

### 誤差の基準を時系列検証値へ切り替えた

画面に出す誤差を、引継ぎ報告値（学習期間と重なる評価）から
ローリング検証（月ごとに「その月より前だけで学習してその月を予測」）の値へ変更した。

| ライン | 変更前（報告値） | 変更後（時系列検証） |
|---|---:|---:|
| GI CH MAE | 6.62 | **6.72**（最悪月 6.92） |
| GL CH MAE | 2.54 | **3.20**（最悪月 4.19） |

GL は 26% 楽観的だった。`ERROR_REFERENCE_REPORTED` に旧値を残し、比較できるようにした。

**影響**: 予測値は変わらない。「差を判別できるか」の判定しきい値が変わるため、
GL で目標との差が 2.54〜3.20 g/m² の範囲にある場合、変更提案の扱いが変わる。

### MAE を根拠に「変更不要」と断定しなくなった

MAE は個々の予測の信頼区間ではないため、それを理由に操業判断を断定していた箇所を修正。

- 追加: `MachineDesignService._resolution()` — 差を `below_mae` / `below_p90` / `above_p90`
  に分類し、**判別できるかどうか**だけを述べる
- 変更: 「条件を変える必要はありません」「これ以上は意味がありません」→
  「モデルはこの大きさの差を判別できません。実機で差が出るかは判断できません」
- 追加: `operational_caution` — 実績外へ出る提案を止めるのは統計ではなく操業判断であることを明示
- 変更: 差が小さくても提案の探索を打ち切らず、必要な条件を提示する

### 組合せとして実績にあるかを見るようにした

項目ごとのレンジ判定だけでは、各項目が実績内でも組合せが実績に無い入力を素通りさせていた。

- 追加: `reference.json` に付着量記号別の `envelope`（平均ベクトル・逆共分散・距離分位点）
- 追加: `RangeChecker.combination_distance()` — 7項目のマハラノビス距離で判定
- 判定は **caution 止まり**（個々の値は実績内のため範囲外とは言えない）

例: GI/Z08 で板厚 1.6 mm は単変量では実績内（P99）だが、組合せとしては距離 6.6（実績99%点 5.7）。

### 名称と表現

- `実機モード` → **`実績モデル`**、`理論モード` → **`理論モデル`**（オンライン接続と誤読されるため）
- 追加: 「このアプリは実機とつながっていません」を使い方タブ冒頭に明示
- 変更: 「いちばん効くのはYノズル位置」→「実績のばらつきを最もよく説明する」＋共線性の注記
- 追加: 提案順（位置→速度→圧力）は**操業方針でモデルの出力ではない**旨を明示（`LEVER_ORDER_BASIS`）
- 変更: GL に理論モデルを使わない理由を、浴温だけでなく浴組成・液体物性・凝固挙動・酸化被膜まで記載
- 変更: 2モデルが近い値でも「正しい」「物理的に妥当」とは言わず、
  **両モデルに共通して入っていない実機要因**（ノズル摩耗・スリット詰まり・板ばたつき・
  エッジオーバーコート・ドロス・付着量計の校正ずれ・搬送遅れ）を列挙

### 機械学習の上乗せ分を4フォールドで測り直した

`tools/rolling_validation.py` を追加し、「記号別中央値のみ」と「配布モデルと同じ構成」を
同一条件のローリング検証（Mar/Apr/May/Jun）で比較した。

| ライン | 記号別中央値のみ | 配布モデル相当 | 改善 |
|---|---:|---:|---:|
| GI | 7.40 | 6.72 | **−0.69 g/m²（9.3%）** |
| GL | 3.29 | 3.20 | −0.09 g/m²（2.7%） |

GL は同一条件の4フォールド比較でも改善が平均 −0.002 g/m²（ほぼゼロ）で、
月ごとの符号も +0.41 / −0.46 / −0.01 / +0.05 と定まらない。
**GLの予測は実質「記号別中央値」**である旨を README と使い方タブに明記した。

### 6月をフォールドに追加しても最悪月は変わらない

従来の Mar/Apr/May に 6月 を足して検証した。6月は GI 6.82 / GL 2.69 で
むしろ良い月であり、最悪月は両ラインとも3月のまま。
表示している最悪月（GI 6.9 / GL 4.2）は妥当と確認できた。

### 誤差が大きくなるコイルを特定した

`tools/error_factors.py` を追加し、アウトオブフォールド誤差をコイル属性で切り分けた。

| 条件 | GI | GL |
|---|---|---|
| 付着量記号が前コイルから変わった直後 | 9.22 vs 7.36（**1.25倍**） | 6.95 vs 3.14（**2.21倍**） |
| 通板時間が短い（下位25%） | 9.27 vs 7.15（1.30倍） | 逆方向（交絡の疑い） |
| 直前から操業条件が変わった | 有意差なし | 4.01 vs 3.09（1.30倍） |

記号切替直後は両ラインとも有意（p&lt;0.0001）に誤差が大きい。使い方タブに注意を追加した。
搬送遅れそのものの補正は、1コイル1行の代表値データではできない（ずらす対象が無い）。

### 検証結果をアプリの挙動へ反映

**モデルの実力を画面に出す（`MODEL_SKILL`）**

「記号別中央値を出すだけ」に対する上乗せ分を、予測カードに常時表示するようにした。

- GI: 7.40 → 6.72 g/m²（9.3%改善）… 通常表示
- GL: 3.29 → 3.20 g/m²（2.7%）… **警告色で「条件を変えたときの予測の動きは実績データから裏づけられていない」と明示**

GL の残差モデルは外していない。外すと予測が条件に一切反応せず、GLの条件設計機能が
成立しなくなるため。代わりに、提案の根拠が弱いことを利用者に見せる方針とした。

**記号切替直後の誤差を実測倍率で補正（`CODE_CHANGE_ERROR_FACTOR`）**

手順1に「直前のコイル：同じ記号／記号が変わる」の切替を追加した。
「記号が変わる」を選ぶと、誤差の見積もりが実測倍率（GI 1.25 / GL 2.21）だけ広がる。

- **予測値は一切変わらない**（テストで差 0.00e+00 を確認）
- 変わるのは参考誤差の表示と、「差を判別できるか」の判定しきい値だけ
- 判定文にも倍率をかけた理由を明記する
- `直前コイルから記号変更` はモデルの入力特徴量ではない（誤差の見積もりにのみ使用）

### 検証中に発見・修正した不具合

**1. Python呼び出しが重なると別の関数の引数で実行される（v4.0からの潜在バグ）**

`py()` は Pyodide の共有グローバル `_arg` に引数を置いて渡していた。呼び出しが重なると、
先の呼び出しが `_arg` を読む前に後の呼び出しが上書きし、**別の関数の引数で実行される**。

起動時に「条件設計の計算」と「装置画面の再現」が同時に走るため、
再現性のある形で発生していた。症状は次のとおり。

- コンソールに `ValueError: ライン区分は GI または GL を指定してください。`
- **起動直後の条件設計タブに答えが表示されない**（`mldesign-out` が空のまま）

`py()` を直列実行に変更して修正。Pyodide は同じスレッドで動くため速度低下はない。
あわせて `mlReady` フラグを追加し、条件の読み込み完了前にタブが開かれても走らないようにした。

**2. ライン・付着量記号を変えても答えが更新されない**

`loadMlDefaults()` が入力欄だけを作り直し、答えを計算し直していなかった。
このため GI→GL と切り替えても、**前のラインの答えが残ったまま**になっていた。
切替後に再計算するよう修正（起動時は `init` が呼ぶので二重実行しない）。

**3. スライダー下限が設備座標の原点まで下がる**

`ui_bounds()` が、実績スパンの25%を引いた結果が負になる項目で下限を 0 に丸めていた。
このため GL で「Yノズル位置を 0 mm へ」という提案が出ていた。
実績最小値の1/2を下限とするよう修正（GI 5.05→2.52 / GL 9.0→4.5 mm）。

### 配布物の棚卸し

- 追加: `.gitignore`、`__pycache__/*.pyc` 42件をリポジトリ管理から除外（動作に影響なし）
- 追加: README に配布物へ含まれる情報の一覧
- 追加: `tools/build_reference_data.py --redact`（任意。既定では動かない）
  件数と生産仕様No.一覧を落とした参照データを生成する。
  予測への影響は実測で GI 0.000 / GL 平均 0.12・最大 0.54 g/m²
- **未実施**: 配布中の `reference.json` は現状のまま。配布方法の判断は運用側に委ねる

### 開発用ツール

- 追加: `tools/evaluate_shipped_model.py` — 配布モデルを月別に評価し、GLの残差寄与を測る
- 追加: `tools/screen_all_columns.py` — ProC 228列を付着量との関係でふるいにかける
