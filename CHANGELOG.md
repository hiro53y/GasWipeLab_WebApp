# CHANGELOG

## v4.4 — 初期画面の固定・速度の実績反映・検証タブの解説・モデル比較タブ

使い勝手の改善。**予測モデル本体（`models/GI_*.json` / `GL_*.json` / `manifest.json`）と
物理モデルは無変更。** 変えたのは初期値・参照データ・画面の説明であって、予測の計算ではない。

### 起動時は必ず「実績モデル ＞ 条件設計」から始まる

前回開いていたタブとモードの記憶をやめた。開いた直後にすることが毎回同じになる。

- `mlState` の既定を `tab:'mldesign'` に変更
- `saveState()` の保存対象から `mode` / `tab` を外した
- `loadState()` は、旧バージョンが保存した `mode` / `tab` を捨ててから復元する
  （捨てないと、既存ブラウザでは保存値に負けて初期画面が変わらない）
- ライン・付着量記号・目標値・記号変更フラグの記憶は今までどおり

### 板厚・板幅を変えると通板速度も実績にあわせて入れ替わる

板厚は速度と強く連動している（実績の相関 GI **−0.863** / GL **−0.688**、
板幅は GI −0.146 / GL −0.416）。サイズだけ変えて速度を記号別中央値のまま置くと、
実在しない組合せの条件になっていた。

速度の当て方を5分割交差検証で比較した（平均絶対誤差 m/min）。

| 方式 | GI | GL |
|---|---:|---:|
| 付着量記号の中央値（v4.3まで） | 18.27 | 17.06 |
| 板厚×板幅グリッド（分位点だけで帯を切る） | 8.56 | 7.80 |
| **板厚×板幅グリッド（分位点＋画面のプリセット値。採用）** | **8.21** | **6.84** |
| 板厚×板幅の近傍25件（不採用・生データ配布が必要） | 7.07 | 6.99 |

記号ごとに表を分けても改善しない（GI 7.02 ≒ 7.07 / GL 7.31 は悪化）ため、ライン単位の表とした。

**帯の境界には画面のプリセット値（板厚 0.27〜3.2 / 板幅 610〜1350）を必ず入れている。**
分位点だけで切ると、点数の少ない厚物が **[1.6, 3.2] mm** という1つの帯に潰れ、
その中央値が 3.2 mm のコイルにも返っていた（GI 63.4 m/min。実測は 1.6〜1.9 mm が 65.0、
1.9〜2.4 mm が 50.0、2.4〜3.2 mm が 40.0、3.2 mm ちょうどが **37.0**）。
プリセットを境界に足すことで、板厚2.3 mm は 63.4 → **50.0** と実測（46.2）に近づいた。

薄物側は非単調のまま（GI で 0.27 mm 115 → 0.40 mm 120）。これは実データの性質で、
板幅1150〜1350帯では 0.25〜0.80 mm の速度がどれも 120〜135 とほぼ横ばい（ラインの上限に張り付いている）。
**平滑化や単調化はしていない。**

- 追加: `tools/build_reference_data.py` の `fit_speed_by_size()` →
  `reference.json` の `lines[line].speed_by_size`（板厚帯×板幅帯の中央値と件数）
- 追加: `RangeChecker.speed_for_size()` — セル → 同じ板厚帯の近い板幅 → 板厚帯 →
  ライン中央値、の順に落とす。**外挿はしない**
- 変更: `RangeChecker.defaults()` — 初期速度も固定サイズ 0.40 mm × 1219 mm の実績から決める
  （旧: 記号別中央値。例えば GI 記号「122」は 57.0 m/min だが、
  0.40 mm × 1219 mm の実績中央値は 125.0 m/min で噛み合っていなかった）
- 追加: `web_api.ml_speed_hint`、`ml_defaults` の戻り値に `speed_hint`
- 画面: 手順3に「板厚 ○ mm × 板幅 ○ mm に近い実績 **n 件**の中央値を入れました」と根拠を表示し、
  **1クリックで直前の速度に戻せる**。実績の外側のサイズでは端の帯の値を出して外側である旨を明示

**予測モデルの入力項目・特徴量・重みは変えていない。変わるのは初期値だけ。**

### 検証タブに「何をどう読むか」を追記

要素は揃っていたが、各カードに読み方の説明が無かった。

- タブ冒頭に読む順番（①予測結果 → ②リスク判定 → ③感度 → ④マップ）
- ①予測結果: 片面／両面の別、適正・未達・過剰の基準（**達成率 90%未満／110%超**）、推定範囲の意味
- ②リスク判定: **「モデル信頼度」と「推定ばらつき ±%」は実測との誤差ではない**ことを明示。
  文献の適用条件からの外れ具合を点数化した見積り（基準 ±12% ＋条件ごとの加算、
  合計 18% 以下＝高／30% 以下＝中／それ超＝低）であり、
  実績モデルの誤差（GI 6.7 / GL 3.2 g/m²）とは別物なので足したり比べたりしないこと
- ③感度グラフ・④マップ: 使い方（管理幅の決め方・運転点の選び方）を追記
- ④計算内訳: 各量の意味と見るべき点（無次元流束 Q は上限 0.6667 に近いほど効いていない、など）
- ⑤モデルからの注意: 自動生成の警告である旨

### 「モデル比較」タブを新設

比較機能自体は v4.2 からあったが、理論モデル＞条件設計タブの下部に隠れており、
計算ボタンを押すまで出てこなかった。

- `tab-modecmp` を新設。**実績モデル側・理論モデル側の両方のメニュー**から開ける
- ボタン1つで「条件を写す → 両方のモデルを計算 → 並べる」まで完結
- **そろえられる条件／そろえられない条件の一覧**を追加（分かりにくさの核心はここだった）
- 追加: 両面合計の作り方の違い（理論は片面×2の対称仮定、実績は CF と CG の独立予測の和）。
  **合計が近くても表裏の配分まで合っている保証はない**
- 修正: ノズル圧力の行が、`importFromMl()` が意図的に写していないにもかかわらず
  差分を数値で出していた。ノズル位置と同じく注記を付け、差を出さないようにした
- 既存の判定文・GL警告・「どちらのモデルにも入っていない実機の変動要因」はそのまま使用

### そのほか

- `sw.js` の `CACHE` を `gaswipelab-v4.4` へ。
  上げないと作り直した `reference.json` が古いままキャッシュに残る
- 使い方タブに ⑧モデル比較 の説明を追加。①条件設計に速度の自動反映の説明を追加
- 修正: 自動で入れた速度が、バーの刻み（0.1）に丸められる前の値のまま条件に入っていた。
  画面が 64.9 なのに計算は 64.92 でする食い違いが起きるため、刻みに合わせてから条件へ入れるようにした
- `tests/test_hmi_screen.py` に4件追加（25 → 30項目）。
  板厚から速度が引けること／実績外でも外挿しないこと／初期速度が固定サイズの実績と一致すること／
  速度の表が無い参照データでも落ちず記号別中央値に戻ること

### 検証結果

```
ゴールデン30件 一致（最大差 5.684e-14）／本家CatBoostパリティ 1000行 一致
物理モデル 34.3 kPa / 8.0 mm（不変）
装置画面テスト 30項目すべてPASS
学習済みモデル7ファイルの SHA-256 が改修前と一致
375px 全9タブ ページ横スクロール0 / DOM ID重複0 / コンソールエラー0
```

## v4.3 — 統計的な言い方の是正と、実績範囲判定の強化

外部レビューの指摘を検証し、妥当と判断したものを反映した。
**予測モデル本体（models/*.json）と物理モデルは無変更。**
ゴールデン30件は最大差 5.684e-14 で一致、物理モデルは 34.3 kPa / 8.0 mm のまま。

### 誤差の基準を時系列検証値へ切り替えた

画面に出す誤差を、引継ぎ報告値（学習期間と重なる評価）から
ローリング検証（月ごとに「その月より前だけで学習してその月を予測」）の値へ変更した。

| ライン | 変更前（報告値） | 変更後（時系列検証） |
|---|---:|---:|
| GI CH MAE | 6.62 | **6.72**（最悪月 6.92） |
| GL CH MAE | 2.54 | **3.20**（最悪月 4.19） |

GL は 26% 楽観的だった。`ERROR_REFERENCE_REPORTED` に旧値を残し、比較できるようにした。

**影響**: 予測値は変わらない。「差を判別できるか」の判定しきい値が変わるため、
GL で目標との差が 2.54〜3.20 g/m² の範囲にある場合、変更提案の扱いが変わる。

### MAE を根拠に「変更不要」と断定しなくなった

MAE は個々の予測の信頼区間ではないため、それを理由に操業判断を断定していた箇所を修正。

- 追加: `MachineDesignService._resolution()` — 差を `below_mae` / `below_p90` / `above_p90`
  に分類し、**判別できるかどうか**だけを述べる
- 変更: 「条件を変える必要はありません」「これ以上は意味がありません」→
  「モデルはこの大きさの差を判別できません。実機で差が出るかは判断できません」
- 追加: `operational_caution` — 実績外へ出る提案を止めるのは統計ではなく操業判断であることを明示
- 変更: 差が小さくても提案の探索を打ち切らず、必要な条件を提示する

### 組合せとして実績にあるかを見るようにした

項目ごとのレンジ判定だけでは、各項目が実績内でも組合せが実績に無い入力を素通りさせていた。

- 追加: `reference.json` に付着量記号別の `envelope`（平均ベクトル・逆共分散・距離分位点）
- 追加: `RangeChecker.combination_distance()` — 7項目のマハラノビス距離で判定
- 判定は **caution 止まり**（個々の値は実績内のため範囲外とは言えない）

例: GI/Z08 で板厚 1.6 mm は単変量では実績内（P99）だが、組合せとしては距離 6.6（実績99%点 5.7）。

### 名称と表現

- `実機モード` → **`実績モデル`**、`理論モード` → **`理論モデル`**（オンライン接続と誤読されるため）
- 追加: 「このアプリは実機とつながっていません」を使い方タブ冒頭に明示
- 変更: 「いちばん効くのはYノズル位置」→「実績のばらつきを最もよく説明する」＋共線性の注記
- 追加: 提案順（位置→速度→圧力）は**操業方針でモデルの出力ではない**旨を明示（`LEVER_ORDER_BASIS`）
- 変更: GL に理論モデルを使わない理由を、浴温だけでなく浴組成・液体物性・凝固挙動・酸化被膜まで記載
- 変更: 2モデルが近い値でも「正しい」「物理的に妥当」とは言わず、
  **両モデルに共通して入っていない実機要因**（ノズル摩耗・スリット詰まり・板ばたつき・
  エッジオーバーコート・ドロス・付着量計の校正ずれ・搬送遅れ）を列挙

### 機械学習の上乗せ分を4フォールドで測り直した

`tools/rolling_validation.py` を追加し、「記号別中央値のみ」と「配布モデルと同じ構成」を
同一条件のローリング検証（Mar/Apr/May/Jun）で比較した。

| ライン | 記号別中央値のみ | 配布モデル相当 | 改善 |
|---|---:|---:|---:|
| GI | 7.40 | 6.72 | **−0.69 g/m²（9.3%）** |
| GL | 3.29 | 3.20 | −0.09 g/m²（2.7%） |

GL は同一条件の4フォールド比較でも改善が平均 −0.002 g/m²（ほぼゼロ）で、
月ごとの符号も +0.41 / −0.46 / −0.01 / +0.05 と定まらない。
**GLの予測は実質「記号別中央値」**である旨を README と使い方タブに明記した。

### 6月をフォールドに追加しても最悪月は変わらない

従来の Mar/Apr/May に 6月 を足して検証した。6月は GI 6.82 / GL 2.69 で
むしろ良い月であり、最悪月は両ラインとも3月のまま。
表示している最悪月（GI 6.9 / GL 4.2）は妥当と確認できた。

### 誤差が大きくなるコイルを特定した

`tools/error_factors.py` を追加し、アウトオブフォールド誤差をコイル属性で切り分けた。

| 条件 | GI | GL |
|---|---|---|
| 付着量記号が前コイルから変わった直後 | 9.22 vs 7.36（**1.25倍**） | 6.95 vs 3.14（**2.21倍**） |
| 通板時間が短い（下位25%） | 9.27 vs 7.15（1.30倍） | 逆方向（交絡の疑い） |
| 直前から操業条件が変わった | 有意差なし | 4.01 vs 3.09（1.30倍） |

記号切替直後は両ラインとも有意（p&lt;0.0001）に誤差が大きい。使い方タブに注意を追加した。
搬送遅れそのものの補正は、1コイル1行の代表値データではできない（ずらす対象が無い）。

### 検証結果をアプリの挙動へ反映

**モデルの実力を画面に出す（`MODEL_SKILL`）**

「記号別中央値を出すだけ」に対する上乗せ分を、予測カードに常時表示するようにした。

- GI: 7.40 → 6.72 g/m²（9.3%改善）… 通常表示
- GL: 3.29 → 3.20 g/m²（2.7%）… **警告色で「条件を変えたときの予測の動きは実績データから裏づけられていない」と明示**

GL の残差モデルは外していない。外すと予測が条件に一切反応せず、GLの条件設計機能が
成立しなくなるため。代わりに、提案の根拠が弱いことを利用者に見せる方針とした。

**記号切替直後の誤差を実測倍率で補正（`CODE_CHANGE_ERROR_FACTOR`）**

手順1に「直前のコイル：同じ記号／記号が変わる」の切替を追加した。
「記号が変わる」を選ぶと、誤差の見積もりが実測倍率（GI 1.25 / GL 2.21）だけ広がる。

- **予測値は一切変わらない**（テストで差 0.00e+00 を確認）
- 変わるのは参考誤差の表示と、「差を判別できるか」の判定しきい値だけ
- 判定文にも倍率をかけた理由を明記する
- `直前コイルから記号変更` はモデルの入力特徴量ではない（誤差の見積もりにのみ使用）

### 検証中に発見・修正した不具合

**1. Python呼び出しが重なると別の関数の引数で実行される（v4.0からの潜在バグ）**

`py()` は Pyodide の共有グローバル `_arg` に引数を置いて渡していた。呼び出しが重なると、
先の呼び出しが `_arg` を読む前に後の呼び出しが上書きし、**別の関数の引数で実行される**。

起動時に「条件設計の計算」と「装置画面の再現」が同時に走るため、
再現性のある形で発生していた。症状は次のとおり。

- コンソールに `ValueError: ライン区分は GI または GL を指定してください。`
- **起動直後の条件設計タブに答えが表示されない**（`mldesign-out` が空のまま）

`py()` を直列実行に変更して修正。Pyodide は同じスレッドで動くため速度低下はない。
あわせて `mlReady` フラグを追加し、条件の読み込み完了前にタブが開かれても走らないようにした。

**2. ライン・付着量記号を変えても答えが更新されない**

`loadMlDefaults()` が入力欄だけを作り直し、答えを計算し直していなかった。
このため GI→GL と切り替えても、**前のラインの答えが残ったまま**になっていた。
切替後に再計算するよう修正（起動時は `init` が呼ぶので二重実行しない）。

**3. スライダー下限が設備座標の原点まで下がる**

`ui_bounds()` が、実績スパンの25%を引いた結果が負になる項目で下限を 0 に丸めていた。
このため GL で「Yノズル位置を 0 mm へ」という提案が出ていた。
実績最小値の1/2を下限とするよう修正（GI 5.05→2.52 / GL 9.0→4.5 mm）。

### 配布物の棚卸し

- 追加: `.gitignore`、`__pycache__/*.pyc` 42件をリポジトリ管理から除外（動作に影響なし）
- 追加: README に配布物へ含まれる情報の一覧
- 追加: `tools/build_reference_data.py --redact`（任意。既定では動かない）
  件数と生産仕様No.一覧を落とした参照データを生成する。
  予測への影響は実測で GI 0.000 / GL 平均 0.12・最大 0.54 g/m²
- **未実施**: 配布中の `reference.json` は現状のまま。配布方法の判断は運用側に委ねる

### 開発用ツール

- 追加: `tools/evaluate_shipped_model.py` — 配布モデルを月別に評価し、GLの残差寄与を測る
- 追加: `tools/screen_all_columns.py` — ProC 228列を付着量との関係でふるいにかける
