"""実機YG装置監視画面の再現（v4.1で追加）。

実機HMIの並びにあわせて、ProCデータ由来の条件と実機モデルの予測を表示するための
読取専用ビュー。予測そのものは行わず、既存の推論結果を並べ替えるだけ。

原則:
    - ProCに存在しない項目は推測で埋めず、未取得（値なし）として返す
    - 確認が取れていない対応関係は unverified を付け、同義扱いしない
    - PLCへの書込みは行わない
"""
